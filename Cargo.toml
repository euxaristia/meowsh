[package]
name = "meowsh"
version = "0.1.0"
edition = "2021"

[dependencies]
libc = "0.2"
rustyline = "17"
glob = "0.3"
globwalk = "0.9"

[profile.release]
lto = true
codegen-units = 1
panic = "abort"

[dev-dependencies]
tempfile = "3.27.0"
