/*
 * meowsh — POSIX-compliant shell
 * builtin_job.c — Job-control builtins (fg bg jobs wait kill)
 *
 * Note: The actual implementations are in builtin_regular.c.
 * This file exists as a placeholder per the project structure.
 * Job control logic is in jobs.c.
 */

#define _POSIX_C_SOURCE 200809L

#include "shell.h"
